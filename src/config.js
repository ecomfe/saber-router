/**
 * @file 配置信息
 * @author treelite(c.xinle@gmail.com)
 */

define({

    /**
     * index文件名
     *
     * @type {string}
     */
    index: '',

    /**
     * 默认的路径
     * 只对hash控制器生效
     *
     * @type {string}
     */
    path: '/',

    /**
     * 默认的根路径
     * 目前只对popstate有意义
     *
     * @type {string}
     */
    root: ''

});
